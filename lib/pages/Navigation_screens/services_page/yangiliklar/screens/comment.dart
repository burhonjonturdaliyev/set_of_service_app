import 'package:flutter/material.dart';

class CommentsNews extends StatelessWidget {
  const CommentsNews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
